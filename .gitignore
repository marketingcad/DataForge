# Dependencies
dataforge-app/node_modules/

# Next.js build output
dataforge-app/.next/
dataforge-app/out/

# Environment variables — never commit these
dataforge-app/.env
dataforge-app/.env.local
dataforge-app/.env.*.local

# Prisma generated client
dataforge-app/src/generated/

# Misc
.DS_Store
*.pem
dataforge-app/.vercel
dataforge-app/next-env.d.ts

# Playwright MCP session logs (contain internal hosts) — never commit
.playwright-mcp/

# Claude Code local settings — machine-specific, and the permission allowlist
# accumulates live URLs/tokens from approved commands (C9: secrets never enter
# committed files).
.claude/settings.local.json
**/settings.local.json
